1. UPDATE пользователь SET дата_регистрации = SUBSTR(дата_регистрации, 7, 4) || '-' || SUBSTR(дата_регистрации, 4, 2) || '-' ||
SUBSTR(дата_регистрации, 1, 2);
2. SELECT логин FROM пользователь ORDER BY дата_регистрации DESC; (первый логин)
3. SELECT DISTINCT(дата_регистрации) FROM пользователь;
4. SELECT SUM(количество) AS 'total_items' FROM товар;
5. SELECT avg(возраст) AS 'средний возраст' FROM пользователь WHERE дата_регистрации < 2021;

